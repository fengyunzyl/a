NAME
  x264

SYNOPSIS
  git make

EXAMPLES
  git
    called by mingw.sh

  make
    called by mingw.sh

OPTIONS
  --enable-static
    library is built by default but not installed

  --enable-win32thread
    avoid installing pthread
