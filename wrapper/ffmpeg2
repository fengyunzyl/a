# Cygwin wrapper for Windows native ffmpeg
# if the path doesnt exist, i need to make it. So i need to either make a file
# or a folder. Which do I make? Well you can make recursive folders with
# "mkdir -p", but can you do that with files?
for q
do
  # FIXME remove this dir afterwards if needed
  mkdir -p -- "$q"
  df -- "$q" | tac >k
  read b <k
  set -- "$q" $b
  x+=("${1/$7/$2/}")
done
rm k
/usr/local/bin/ffmpeg "${x[@]}"
